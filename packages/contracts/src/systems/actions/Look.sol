// SPDX-License-Identifier: MIT
pragma solidity >=0.8.21;

import { ErrCodes, ResCodes } from '../../constants/defines.sol';
//import { CommandLookups } from '../CommandLookup.sol';

import {ActionType, RoomType, ObjectType, CommandError, DirectionType} from "../../codegen/common.sol";

library Look {
    
    //function fishDirectionTok(string[] memory tokens, CommandLookups luts) 
    //public view returns (string memory, uint8) {

        //string memory tok;
        //uint8 err_code;
        //if (luts.cmdLookup(tokens[0]) != ActionType.None) {
            /* Look form
            *
            * look_cmd = look 
            *
            */
        //} else  {
            //err_code = ErrCodes.ER_PR_NOP; 
        //}
    //}


    //function look(string[] calldata tokens, CommandLookups luts) public returns (string memory, uint8) {
        ////return  fishDirectionTok(tokens, caller);
        //return ("foo", 0);
    //}
}


