import { entityKind } from '../entity.js';
import type { Logger } from '../logger.js';
import { PgDatabase } from '../pg-core/db.js';
import { PgDialect } from '../pg-core/dialect.js';
import { type RelationalSchemaConfig, type TablesRelationalConfig } from '../relations.js';
import { type DrizzleConfig } from '../utils.js';
import { type NeonHttpClient, type NeonHttpQueryResultHKT, NeonHttpSession } from './session.js';
export interface NeonDriverOptions {
    logger?: Logger;
}
export declare class NeonHttpDriver {
    private client;
    private dialect;
    private options;
    static readonly [entityKind]: string;
    constructor(client: NeonHttpClient, dialect: PgDialect, options?: NeonDriverOptions);
    createSession(schema: RelationalSchemaConfig<TablesRelationalConfig> | undefined): NeonHttpSession<Record<string, unknown>, TablesRelationalConfig>;
    initMappers(): void;
}
export type NeonHttpDatabase<TSchema extends Record<string, unknown> = Record<string, never>> = PgDatabase<NeonHttpQueryResultHKT, TSchema>;
export declare function drizzle<TSchema extends Record<string, unknown> = Record<string, never>>(client: NeonHttpClient, config?: DrizzleConfig<TSchema>): NeonHttpDatabase<TSchema>;
