import { entityKind } from '../../entity.js';
import type { SelectResultFields } from '../../query-builders/select.types.js';
import { QueryPromise } from '../../query-promise.js';
import type { Query, SQL, SQLWrapper } from '../../sql/index.js';
import type { SQLiteDialect } from '../dialect.js';
import type { PreparedQuery, SQLiteSession } from '../session.js';
import { SQLiteTable } from '../table.js';
import type { InferModel } from '../../table.js';
import { type DrizzleTypeError, type Simplify } from '../../utils.js';
import type { SelectedFieldsFlat, SelectedFieldsOrdered } from './select.types.js';
export interface SQLiteDeleteConfig {
    where?: SQL | undefined;
    table: SQLiteTable;
    returning?: SelectedFieldsOrdered;
}
export interface SQLiteDelete<TTable extends SQLiteTable, TResultType extends 'sync' | 'async', TRunResult, TReturning = undefined> extends SQLWrapper {
}
export declare class SQLiteDelete<TTable extends SQLiteTable, TResultType extends 'sync' | 'async', TRunResult, TReturning = undefined> extends QueryPromise<TReturning extends undefined ? TRunResult : TReturning[]> implements SQLWrapper {
    private table;
    private session;
    private dialect;
    static readonly [entityKind]: string;
    constructor(table: TTable, session: SQLiteSession<any, any, any, any>, dialect: SQLiteDialect);
    where(where: SQL | undefined): Omit<this, 'where'>;
    returning(): SQLiteDelete<TTable, TResultType, TRunResult, InferModel<TTable>>;
    returning<TSelectedFields extends SelectedFieldsFlat>(fields: TSelectedFields): SQLiteDelete<TTable, TResultType, TRunResult, Simplify<SelectResultFields<TSelectedFields>>>;
    toSQL(): Omit<Query, 'typings'>;
    prepare(isOneTimeQuery?: boolean): PreparedQuery<{
        type: TResultType;
        run: TRunResult;
        all: TReturning extends undefined ? DrizzleTypeError<'.all() cannot be used without .returning()'> : TReturning[];
        get: TReturning extends undefined ? DrizzleTypeError<'.get() cannot be used without .returning()'> : TReturning | undefined;
        values: TReturning extends undefined ? DrizzleTypeError<'.values() cannot be used without .returning()'> : any[][];
        execute: TReturning extends undefined ? TRunResult : TReturning[];
    }>;
    run: ReturnType<this['prepare']>['run'];
    all: ReturnType<this['prepare']>['all'];
    get: ReturnType<this['prepare']>['get'];
    values: ReturnType<this['prepare']>['values'];
    execute(placeholderValues?: Record<string, unknown>): Promise<TReturning extends undefined ? TRunResult : TReturning[]>;
}
